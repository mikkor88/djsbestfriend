<%= f.inputs do %>
	<%= f.input :title %>
	<%= f.input :grouping %>
	<%= f.input :year, :as => :date,		 :discard_day => true, :discard_month => true, :start_year => Time.now.year,
																			 :end_year => Time.now.year-10, :include_blank => false  %>
	<%= f.input :description, :input_html => { :class => 'comment' } %>
<% end %>